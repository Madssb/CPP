#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>     //defines the standard stream objects that input and output data.
#include <algorithm>
using namespace std; 


int main() {
    /*
    int a; //declares variables as integers
    int b;
    int c;
    */
    int a, b, c; //accomplishes the same  in less lines
    cin >> a >> b >> c; //assigns inputs to variables
    cout << a+b+c << endl; // returns sum of variables
    return 0;
}