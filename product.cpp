#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>     //defines the standard stream objects that input and output data.
#include <algorithm>
using namespace std; 


int main() {
    int a, b; //accomplishes the same  in less lines
    cout << "enter 2 integers you wish to multiply:" << endl;
    cin >> a >> b; //assigns inputs to variables
    cout  << "the product is " << a*b << "." << endl; // returns sum of variables
    return 0;
}